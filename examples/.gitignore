node_modules
dist
# Staged CDN snapshot — a build artifact regenerated by `npm run stage:cdn`
# from the baked ../cdn tree. Don't commit the (large, gzipped) shards.
public/cdn
*.local
.DS_Store
*.tsbuildinfo
