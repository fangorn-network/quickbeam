# 🌲 Sherwood Chain [4663] Topology | Block: #11,427,667

> **Network State Summary:** The headline finding is that **~27% of all measured on-chain USD flow ($1.12M of $4.18M) terminates in a single address, `0x1A18a8b9…`, fed by a deterministic mint→relay→burn pipeline that begins at the zero address** — this is issuer supply machinery being re-counted as volume at every hop, not organic trading. The mega-cap nodes the market watches (**`TSLA`**, **`AAPL`**, **`NVDA`**, **`GOOGL`**, **`AMD`**) are effectively **dormant** in the current window (~$0.90 of recent volume each), while genuine flow is concentrated in **`USO`**, **`BABA`**, and **`SGOV`**.

**⚠️ Data integrity caveats — read before acting on anything below:**


*   **Corpus:** 17,507 records — 15,051 Transfers, 1,378 Wallets, 1,078 Assets (98 symbols × 11 snapshots).
*   **Window:** 2026-06-26 11:32 UTC → 2026-07-16 17:12 UTC (~20 days), blocks **227,770 → 11,427,667**. *This is a 20-day corpus, not a 24h window.*
*   **Asset snapshot:** observed 2026-07-16 17:04 UTC.
*   **Total transfer value:** **$4,184,410.96** across 98 symbols.
*   **Publisher:** `0x147c24c5…45Ef6` (all records).

---

## 🪙 1. Tokenized Stock Snapshots (RWA Nodes)

Latest snapshot (17:04 UTC). `marketCap`, `holders`, `totalSupply`, `recentVolume` measure the **tokenized wrapper only**, never the underlying company.

### The requested nodes — all three are dormant or broken

| Token Node | Contract Address | On-Chain Price | Recent Vol (USD) | Holders | Wrapper Mcap | Premium/Discount |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **`NVDA`** | `0x465834D5…A492` | 🔴 **none** | 🔴 **none** | 156 | 🔴 **none** | ⛔ n/a |
| **`TSLA`** | `0x322F0929…3b2d` | $406.30 | **$0.89** | 7,758 | $909,213 | ⛔ n/a |
| **`AAPL`** | `0xaF3D76f1…93f9` | $336.62 | **$0.91** | 10,639 | $610,557 | ⛔ n/a |

**🟡 `NVDA` is not a tokenized NVIDIA equity.** Its record carries **no price and no market cap**, a `totalSupply` of exactly **1,000,000,000**, and a `recentVolume` of **1,325,220,821 tokens — more than its entire supply**. A 1e9 round supply with no exchange rate is the signature of an imposter/meme token squatting the ticker, not a Robinhood-issued RWA wrapper. The pipeline's own text field still describes it as "a tokenized semiconductors stock," which is a **labeling bug worth fixing upstream** — the description is inherited from the ticker, not verified against the contract. Do not trade this on ticker match.

**🔴 `TSLA` / `AAPL` / `AMD` / `GOOGL` are flat-lined.** Each shows `circularityRatio` **0**, `senderHHI` **1.0**, and sub-$1 recent volume — meaning their recent window contains exactly **one** dust transfer from a single sender. Despite 7.7k–10.6k holders, there is **no live flow** in these nodes right now. (Earlier snapshots in the corpus show `TSLA` at $105k volume; that window has since rolled off. Ranking these off max-across-snapshots would be a stale read — a trap worth flagging.)

### Where the flow actually is

| Token Node | Price | Recent Vol (USD) | Net Flow (USD) | Holders | Wrapper Mcap | Circularity | Manip. Score |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: | :---: |
| **`USO`** | $124.26 | **$868,841** | 🟢 +$210,787 | 73 | $479,824 | 0.757 | 0.371 |
| **`BABA`** | $112.86 | **$471,585** | 🟢 +$116,823 | 25 | $177,815 | 0.752 | 0.387 |
| **`SGOV`** | $100.65 | **$297,070** | 🟢 +$96,711 | 86 | $808,044 | 0.674 | 0.381 |
| **`TSM`** | $408.84 | $160,057 | 🟢 +$30,748 | 34 | $27,427 | 0.808 | 0.392 |
| **`EWY`** | $182.36 | $111,536 | 🟢 +$21,516 | 16 | $21,689 | 0.807 | 0.391 |
| **`NFLX`** | $71.00 | $107,390 | 🟢 +$18,146 | 29 | $24,862 | 0.831 | 0.396 |
| **`MSTR`** | $92.32 | $93,982 | 🟢 +$10,580 | 23 | $10,730 | 0.887 | 0.409 |
| **`SPY`** | $755.25 | $11,944 | 🟢 +$2,213 | 2,604 | $550,388 | 0.815 | 0.385 |
| **`SLV`** | $52.87 | $13,981 | 🟡 **+$1.44** | 115 | $447,301 | 🔴 **0.9999** | 🔴 0.496 |
| **`META`** | $672.67 | $2,521 | 🟡 **+$5.11** | 5,763 | $482,458 | 🔴 **0.998** | 🔴 0.495 |

**🔴 `SLV` and `META` are round-trippers.** $13,981 and $2,521 of gross volume producing **net flow of $1.44 and $5.11** — a circularity ratio of 0.9999/0.998 means essentially every token that moves comes back. This is volume without position change. `SLV`'s 11 distinct counterparties against 115 holders confirms a closed loop.

### ⛔ Premium/Discount vs. TradFi — NOT COMPUTABLE, NOT ESTIMATED

The template requests a premium/discount column. **It cannot be honestly filled from this corpus, and I have not invented one.** The `price` field is sourced from Blockscout's `exchange_rate`, which *mirrors the real-world equity price*. There is no independent on-chain pool swap rate in the dataset to compare it against, so any "premium" computed here would be **structurally zero by construction** — an artifact of comparing a number to itself, not a market signal. Reporting arbitrage spreads off this data would be fabrication.

**To make §1's premium column real, the pipeline needs a pool-derived execution price** (see §2).

---

## 🌊 2. ERC-20 Transfer Flow Vectors (Edges)

### ⛔ Uniswap V3/V4 Swap Flows — NO DATA IN CORPUS

The corpus contains **no pool, TVL, liquidity, swap, tick, or fee-tier fields**. Every one of the 15,051 Transfer records carries `actionType: "transfer"` and `signal: "notable-transfer"` — there is not a single swap-classified event in the dataset. The field vocabulary is transfer-shaped end to end (`fromAddr`, `toAddr`, `value`, `usdValue`, `txHash`, `blockNumber`).

**Total swap volume, net buy/sell legs, dominant pool, and pool share cannot be reported.** They are not "currently zero" — they are **not measured**. Any figure in these slots would be invented.

### ⛔ Bridge Transfers — NO DATA IN CORPUS

No canonical-bridge address tagging, no L1↔L2 event classification, no ETH/USDC inflow tracking. Arbitrum bridge activity **cannot be assessed**.

### ✅ Mint/Burn Flow — MEASURED, and it dominates the network

This is the one part of §2 the data genuinely supports, and it is the report's central finding.

*   **Mint:** `0x0000…0000` → **486 transfers, $829,090.11, across 76 symbols**, to just **2** recipients.
*   **Burn:** `0xa8553DB0…3C74` → `0x0000…0000` — **$191,311.53** returned to the zero address.
*   **Net:** the zero address is a **net issuer of ~$638k** over the window.

Mint/burn is not a side note here. **The minting pipeline is the single largest flow structure on the chain**, and because each hop re-counts the same tokens, it inflates every downstream volume metric.

---

## 🐳 3. High-Velocity Addresses & Whale Movements

### 🚨 The relay chain: one pipeline, counted four times

Every hop below moves **the same tokens** across **76 symbols**, each hop re-counted as fresh volume:

```
0x0000…0000  (mint)
    │  486 tx · $829,090.11 · 76 symbols · → exactly 2 peers
    ▼
0xcfAEce21…0A94  (relay 1 — receives from 1 source only)
    │  481 tx · $829,102.93 · 76 symbols · → exactly 1 peer
    ▼
0x1A18a8b9…A4E7  (hub — $1,119,808.60 in = 26.8% of ALL network USD)
    │  437 tx · $637,491.96 · 73 symbols · → 5 peers
    ├──► 0x9f736F87…F1ae   $197,163.57  (2 tx)
    ├──► 0xeF5ca6b3…cBD4   $182,100.06  (3 tx)
    ├──► 0xfac1d7dC…0F16   $140,288.13  (368 tx, 67 symbols → 1 peer)
    ├──► 0xa8553DB0…3C74   $116,755.53  (5 tx) ──► 0x0000 (BURN $191,311.53)
    └──► 0xA9877C8A…8b23     $1,184.67  (59 tx, 59 symbols)
```

`0xcfAEce21…` forwards **$829,102.93** after receiving **$829,090.11** — a **99.998% pass-through ratio**. It holds nothing. It is a conduit.

### 🔬 Proof of pass-through: the `BABA` hop (amounts identical to 8 decimals)

| # | From → To | Token | Amount | USD | Block | Time | Tx |
| :---: | :--- | :---: | :--- | :--- | :--- | :--- | :--- |
| 1 | `0x9f736F87…` → `0xbeD9Ae2C…` | **`BABA`** | **1,034.393665** | $116,741.67 | 8,715,005 | 13:47 UTC | `0xa5454205…5822` |
| 2 | `0xbeD9Ae2C…` → `0x1A18a8b9…` | **`BABA`** | **1,034.393665** | $116,741.67 | 8,715,171 | 13:48 UTC | `0x46ee795e…d9f7` |

**166 blocks (~1 minute) apart. Identical to the eighth decimal.** `0xbeD9Ae2C…` did not accumulate `BABA` — it held it for sixty seconds and passed 100% of it onward.

### The "whales" are relay nodes, not traders

| Address / Node | Type | Token | Net Amount | Tx Hash | Flow Behavior |
| :---: | :--- | :---: | :--- | :--- | :--- |
| `0x1A18a8b9…A4E7` | 🔴 **Pipeline hub** | 76 symbols | 🟢 in **$1,119,808.60** | *(483 tx)* | Terminal sink of the mint chain. **26.8% of all network USD.** Only 2 sources. Not a market participant. |
| `0xcfAEce21…0A94` | 🔴 **Conduit** | 76 symbols | ↔ **$829,102.93** | *(481 tx)* | 99.998% pass-through, mint → hub. Holds nothing. |
| `0xbeD9Ae2C…C85F` | 🟡 **Apparent whale — is a relay** | **`USO`**, **`BABA`** | ↔ **$290,705.67** | `0x46ee795e…d9f7` | Highest USD-per-tx on chain (2 tx). **Both legs pass straight through.** Received $290,705.67 from `0x9f736F87…` — the *same* amount it forwarded. **Do not read as accumulation.** |
| `0x6d56Ab47…a158` | 🟢 **Distributor (genuine breadth)** | 70 symbols | 🔴 out **$207,733.97** | *(893 tx)* | **223 distinct recipients** — the only high-volume node with real fan-out. Closest thing to organic distribution. |
| `0x006102b1…24fa` | 🟢 **Aggregator (genuine breadth)** | 77 symbols | 🟢 in **$141,967.28** | *(531 tx)* | **186 distinct sources** across 76 symbols. Broad, many-to-one collection. Organic-shaped. |
| `0xfac1d7dC…0F16` | 🔴 **Conduit** | 70 symbols | 🟢 in **$183,168.82** | *(650 tx)* | 650 transfers, 70 symbols, **exactly 1 outbound peer**. Pipeline leg. |

**The distinguishing test is counterparty breadth, not USD size.** `0x1A18a8b9…` moves 5× the USD of `0x6d56Ab47…` through **2** counterparties; `0x6d56Ab47…` uses **223**. Size ranks the pipeline first. Breadth ranks the real activity first.

---

## 🚨 4. Systemic Anomalies & Arbitrage Triggers

### ⛔ On-Chain Premium Arbitrage — NO ARBITRAGE DETECTABLE

**There is no premium to trade, and none is reported.** As established in §1, `price` mirrors the real equity feed; the corpus has no pool execution price. The "24/7 advantage" thesis **cannot be evaluated with this data** — not because the spread is zero, but because the second leg of the comparison does not exist in the dataset.

### 🔴 Volume Authenticity — the real systemic anomaly

Applying the skill's authenticity checks, the failures are structural:

*   **Circular flow:** `SLV` (circ **0.9999**, net **$1.44** on $13,981 gross), `META` (**0.998**, net **$5.11**), `TSLA` (**0.9968**), `AVGO` (**0.9925**), `SPY` (**0.9957**), `AAPL` (**0.9804**). Volume that nets to nothing.
*   **Same wallet set across *different* assets:** the relay chain touches **76 of 98 symbols**. One actor, not 76 markets.
*   **A `0x0000…` mint feeding the chain:** confirmed — it is the chain's origin.
*   **Amount quantization:** `BE` at **0.97**, `WEEK` **0.25**, `ROBIN` **0.20**, `PEPEROBIN` **0.025** — repeating rounded values, a scripted-transfer signature.
*   **Highest manipulation scores:** `SNDK` **0.552**, `BE` **0.544**, `AAPL` **0.540**, `SLV` **0.496**, `META` **0.495**, `TSLA` **0.493**.

**Adjusted view:** strip the $829k relay pipeline and the ~$4.18M headline drops by **~20%** on the mint chain alone, before accounting for round-trip volume in the high-circularity names. **Treat the network's stated volume as an upper bound with a large synthetic component.**

### 📉 Liquidity Slippage Warnings

⛔ **Cannot be computed.** Slippage and price-impact thresholds require pool depth. No TVL data exists in the corpus (§2). **No slippage figures are given because any would be fabricated.**

The `holders`/`marketCap` fields are wrapper-adoption signals, not liquidity — but the thinness they imply is worth stating: `TSM` carries a **$27,427** wrapper market cap against **$160,057** of recent volume (**5.8× turnover of its own float**), and `MSTR` **$10,730** mcap against **$93,982** volume (**8.8×**). Volume far exceeding float is itself circularity evidence, consistent with their 0.81/0.89 circularity ratios.

---

## 🏹 5. Tactical On-Chain Playbooks

**The honest tactical read: this corpus does not support trade execution.** The template's three plays each require data the dataset lacks. Rather than dress up invented numbers as signals, here is what the data *does* support.

*   ⛔ **Play 1 (Arbitrage) — CANNOT BE ISSUED.** Requires a pool price independent of the equity feed. Does not exist here. Any discount % quoted would be fiction.
*   ⛔ **Play 2 (Liquidity Provision) — CANNOT BE ISSUED.** Requires pool TVL and fee-tier data. Does not exist here. Additionally, the "volume breakout" such a play would chase is **substantially the mint relay** — supplying liquidity against synthetic volume is precisely the trap this analysis surfaces.
*   ⛔ **Play 3 (Whale Tracking) — ACTIVELY HARMFUL AS FRAMED.** The top USD addresses are **not whales**; they are issuer plumbing (§3). Their transfers are mechanical, not predictive. "Their past 3 swaps preceded major moves" is unsupported — the corpus contains **no swaps at all**, and `0xbeD9Ae2C…`'s flow provably passes through in ~60 seconds. Copy-trading these addresses would be following a mint script.

### 🟩 What the data actually supports

*   🟩 **Discount all volume metrics by counterparty breadth before ranking anything.** `distinctCounterparties` and `senderHHI` separate the two `0x1A18a8b9`-class conduits from genuine distributors like `0x6d56Ab47…` (223 peers). Volume alone ranks the pipeline first — this is the corpus's main analytical trap.
*   🟩 **Treat `USO`, `BABA`, `SGOV` as the only nodes with meaningful net flow** (+$210,787 / +$116,823 / +$96,711) — though note `USO` and `BABA` also carry the largest relay legs, so even these deserve a per-hop netting pass before use.
*   🟩 **Flag `NVDA` for pipeline correction.** A 1e9-supply, no-price, no-mcap token is being labeled a "tokenized semiconductors stock" from ticker match alone. This is a **data-quality defect that will mislead any downstream consumer** — it should be fixed at ingest, not worked around by readers.
*   🟩 **Do not read the mega-caps as liquid.** `TSLA`/`AAPL`/`AMD`/`GOOGL` show ~$0.90 recent volume against 7k–10k holders. Distribution without flow.

### 🔧 Pipeline remediation (blocking better analysis)

1.  🔴 **Restart the watcher** — `./watch_robinhood.sh` is not running; the embed→Qdrant→CDN stage has been stalled since ~17:12 UTC, which is why the corpus is 6.8h behind the chain tip.
2.  🟡 **To unlock §1/§2/§4/§5**, ingest needs Uniswap V3/V4 `Swap` events (pool address, sqrtPriceX96 → execution price, tick liquidity → TVL, fee tier). That single addition converts four of this report's ⛔ sections into real signal, and would make premium/discount a genuine measurement rather than a tautology.
3.  🟡 **Tag bridge and known-router addresses at ingest** so mint/relay/burn plumbing is classified rather than re-discovered as "whales" by every downstream consumer.
4.  🟡 **Verify ticker→contract mapping** against an issuer allowlist to catch imposters like the `NVDA` node.

---

### 📌 Provenance

All figures derive from the `robinhood` fangorn corpus, publisher `0x147c24c5…45Ef6`. Verifiable source CIDs for the load-bearing claims:

*   `BABA` relay hop, leg 1 — `bafyreicetfznsut5zlg6vwg2yhiswjpxkp4go344fqf5y72gxbyjtfyuci`
*   `BABA` relay hop, leg 2 — `bafyreiht7d34pwfjtq47okqvvyo7e4mwk6neiighiomdrrbt36pvwuwwsy`
*   `USO` relay leg (`0xcfAEce21…` → `0x1A18a8b9…`, 1,084.15 USO / $121,457) — `bafyreiemomnbucnqvd4vuyplcrsbjrjnwjitviqdv2ulk37vdk7kvjoh3q`

**Report generated 2026-07-16 23:59 UTC against data current to 17:12 UTC.**
